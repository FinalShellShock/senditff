# Firebase web config (public-by-design — set these in Vercel env vars too)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=

# Anthropic API key (server-side only — set in Vercel, never VITE_ prefix)
ANTHROPIC_API_KEY=

// Stamp on every logged feedback entry so tuning data stays attributable to
// the engine that produced it. Bump when the algorithm changes materially.
// Formation lineage: Pro Set -> Spread -> Audible -> West Coast -> Shotgun.
export const ALGO_VERSION = "shotgun";
